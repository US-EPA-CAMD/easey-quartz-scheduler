applications:
  - name: ((name))
    memory: ((memory))
    instances: ((instances))
    buildpacks:
      - dotnet_core_buildpack
    env:
      CACHE_NUGET_PACKAGES: false
    routes:
      - route: ((host))/((path))
    services:
      - camd-pg-db